name: CI build
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          submodules: true
      - uses: purcell/setup-emacs@master
        with:
          version: 24.5
      - run: ./do.sh
