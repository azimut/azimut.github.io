{
  "scripts": {
    "release": "postcss *.css --dir public/",
    "dev": "concurrently \"live-server public/\" \"postcss *.css -w --dir public/\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "live-server": "^1.2.2",
    "postcss-cli": "^11.0.0",
    "postcss-nested": "^7.0.2"
  }
}
